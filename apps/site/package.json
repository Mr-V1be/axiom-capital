{
  "name": "@axiom/site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm run build -w @axiom/web && node worker/build-worker.mjs",
    "build:worker": "npm run build",
    "preview": "wrangler dev",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}
